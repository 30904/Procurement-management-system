# API base — see `src/services/api.js` `getApiRoot()` for rules.
# Development: http://localhost:5020  or leave empty to use Vite proxy.
# Production (https://pms.idms-atp.com): leave empty — same-origin /api behind Nginx.
VITE_API_BASE_URL=

# Shown in the app header after login
# VITE_COMPANY_NAME=Your Company Pvt Ltd

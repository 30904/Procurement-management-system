/**
 * When true, every user sees Purchase Dashboard from the sidebar Dashboard menu.
 * Set to false once roles are mapped via Configuration → Role Dashboard Mapping.
 */
export const TEMPORARY_FORCE_PURCHASE_DASHBOARD = true;
